import portraitAsset from "@/assets/yash-portrait.png.asset.json";
import curiosityAsset from "@/assets/curiosity-ai.png.asset.json";
import niorAsset from "@/assets/nior-store.png.asset.json";

// Images are hosted on the site's own CDN so they always load.
export const PORTRAIT_IMAGE = portraitAsset.url;
export const CURIOSITY_IMAGE = curiosityAsset.url;
export const NIOR_IMAGE = niorAsset.url;
